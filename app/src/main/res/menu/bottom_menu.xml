<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/item_homePage"
        android:enabled="true"
        android:icon="@drawable/color_home_icon"
        android:title="홈화면" />

    <item android:id="@+id/item_matchPage"
        android:enabled="true"
        android:icon="@drawable/colored_heart_icon"
        android:title="매칭화면" />

    <item android:id="@+id/item_searchPage"
        android:enabled="true"
        android:icon="@drawable/colored_search_icon"
        android:title="검색화면" />

    <item android:id="@+id/item_myPage"
        android:enabled="true"
        android:icon="@drawable/color_person_icon"
        android:title="프로필화면" />

</menu>